## Summary
<!-- What changed and why -->

## Changes
<!-- Bullet list of files/areas modified -->

## Test plan
- [ ] Backend tests pass (`uv run pytest`)
- [ ] Backend lint passes (`uv run ruff check . && uv run ruff format --check .`)
- [ ] Frontend lint passes (`npm run lint`)
- [ ] Frontend build passes (`npm run build`)
- [ ] Tested on mobile viewport (360x640)

## Checklist
- [ ] No hardcoded strings (all text via next-intl)
- [ ] No new ChromaDB references (pgvector only)
- [ ] API keys remain server-side only
- [ ] Sources cited in any AI-generated content
